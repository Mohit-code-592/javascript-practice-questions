//check a character is vowel or consonant 
let char = 'g';
if (char === 'i' || char === 'a' || char === 'u' || char === 'e' || char === 'o') {
    console.log(`chat is vowel`);
}
else {
    console.log(`char is consonant`);
}
